{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://normco.re/schemas/feed.json",
  "title": "JSON Feed",
  "description": "JSON Feed 1.1 output contract for normco.re feeds.",
  "type": "object",
  "required": ["version", "title", "items"],
  "properties": {
    "version": {
      "type": "string",
      "pattern": "^https://jsonfeed\\.org/version/1"
    },
    "title": {
      "type": "string",
      "minLength": 1
    },
    "description": {
      "type": "string"
    },
    "home_page_url": {
      "type": "string",
      "format": "uri"
    },
    "feed_url": {
      "type": "string",
      "format": "uri"
    },
    "language": {
      "type": "string",
      "minLength": 2
    },
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "title"],
        "properties": {
          "id": { "type": "string" },
          "url": { "type": "string" },
          "title": { "type": "string", "minLength": 1 },
          "summary": { "type": "string" },
          "content_html": { "type": "string" },
          "date_published": { "type": "string", "format": "date-time" },
          "tags": {
            "type": "array",
            "items": { "type": "string" }
          }
        }
      }
    }
  }
}
