# Build and deploy static content to Aliyun OSS
name: Deploy static content to OSS

on:
  push:
    branches: ["master"]
    paths:
      - ".github/workflows/pretext-harness.yml"
      - ".github/workflows/site.yml"
      - ".tool-versions"
      - "_config.ts"
      - "_config/**"
      - "contracts/**"
      - "deno.jsonc"
      - "deno.lock"
      - "src/blog/client/deno.json"
      - "src/blog/client/deno.lock"
      - "package.json"
      - "package-lock.json"
      - "plugins/**"
      - "pnpm-lock.yaml"
      - "scripts/**"
      - "src/**"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: "oss-deploy"
  cancel-in-progress: true

jobs:
  pretext-harness:
    uses: ./.github/workflows/pretext-harness.yml
    with:
      runs-on: macos-26

  deploy:
    needs: pretext-harness
    environment:
      name: development
    runs-on: macos-26
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          # Full history is required for build-time per-file git metadata.
          fetch-depth: 0

      - name: Setup Deno environment
        uses: ./.github/actions/setup-deno

      - name: Verify frozen lockfiles
        run: deno task locks:check

      - name: Deploy to Aliyun OSS
        uses: frenchvandal/aliyun-oss-cdn-sync-action@master
        with:
          role-oidc-arn: ${{ secrets.ALIBABA_CLOUD_ROLE_ARN }}
          oidc-provider-arn: ${{ secrets.ALIBABA_CLOUD_OIDC_PROVIDER_ARN }}
          role-session-name: ${{ github.run_id }}
          role-session-expiration: 3600
          cache-enabled: true
          cache-key: >-
            lume-cache-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('deno.lock') }}-${{ hashFiles('_cache/**/*') || 'empty' }}
          cache-restore-keys: |
            lume-cache-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('deno.lock') }}-
            lume-cache-${{ runner.os }}-${{ runner.arch }}-
          input-dir: _site
          bucket: ${{ secrets.OSS_BUCKET }}
          region: ${{ secrets.OSS_REGION }}
          audience: ${{ github.repository_id }}
          cdn-enabled: true
          cdn-base-url: ${{ secrets.OSS_CDN_BASE_URL }}
          cdn-actions: refresh
          build-command: deno task --frozen build
